import register from './modules/register';
import restartPassword from './modules/restartPassword';

const templates = () => {
  return {
    register,
    restartPassword
  };
};

export default templates;
